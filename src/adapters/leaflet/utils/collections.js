/**
 * Backwards-compatible re-exports for Leaflet adapter.
 * The implementation is moved to src/adapters/shared/collections.js.
 */

export { DEFAULT_GEOMETRY, resolveCollection } from "../../shared/collections.js";

